#include <iostream>
using namespace std;

int fct(int n)
{
    if(n == 2)
        return 2;
    return n * fct(n-1);
}
int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
    int n;
    cin >> n;
    cout << fct(n);
}